<%= flash_messages %>

<h2>Jobs List</h2>


<% if false %>
here we are showing the search functionality
<% end %>

       <div class="col-md-4" style="float:right">
          <h4>Search a Job by keyword </h4>
           <%= simple_form_for :search, url: :jobs, method: "GET", html: { class: 'form-inline' } do |f| %>
           <%= f.input :title,input_html: {value: ""} %>
           <%= f.submit "Search", class: "btn btn-primary" %>
          <% end %>
        </div>


        <br><br>

<div class="job-block">
  <table class="table table-striped table-bordered table-hover">
    <thead>
      <tr>

        <th class="col-md-2">Title</th>
        <th class="col-md-2">Creator</th>
        <th class="col-md-4">Options</th>
      </tr>
    </thead>

    <tbody>
      <% @jobs.each do |job| %>
        <tr>

          <td class="col-md-2"><%= job.title %></td>
          <td class="col-md-2"><%= job.user.try(:email) %></td>
          <td class="col-md-4">
            <%= link_to 'Show', job_path(job), class: "btn btn-info" %>
            <%= link_to 'Edit', edit_job_path(job), class: "btn  btn-warning" %>
            <%= link_to 'Destroy', job_path(job), method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

<br>

<%= link_to 'New Job', new_job_path, class: "btn btn-primary" %>

</br></br>

<% if false %>
here we are showing the scrapping result
<% end %>

</div>
      <div class="col-md-4">

         <h3><%= @main %> </h3></br>
         <p > <%= @paragraph %> </p>
         <a href="<%= @href %>">Read More here</a>

       </div>

</div>
